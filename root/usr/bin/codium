#! /bin/bash

BIN=/usr/share/codium/bin/codium

# Wayland check
if [ "${PIXELFLUX_WAYLAND,,}" == "true" ]; then
  WAYLAND="--ozone-platform=wayland"
fi

${BIN} \
  --no-sandbox \
  ${WAYLAND} \
   "$@"
